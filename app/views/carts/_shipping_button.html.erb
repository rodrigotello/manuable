<% if c.shipping_boolean == true %>
	<%= link_to 'Envio agregado', shipping_local_path(id: c.id), class: 'shipping-on', remote: true %>
<% else %>
	<%= link_to 'Agregar envio', shipping_local_path(id: c.id), class: 'shipping-off', remote: true %>
<% end %>
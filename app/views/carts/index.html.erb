<div class="container">
	<div class="checkout-wrapper">
		<h2 class="mi-carrito">Mi carrito</h2>
		<p class="mi-carrito">Recuerda revisar todos los antes de pasar a pagar. Puedes usar tarjeta de crédito, débito, transferencia bancaria u OXXO.</p>
		<div class="cart-products-box">
			<% @total = 0 %>
			<% @carts.each do |c| %>
				<div class="cart-item-divider"></div>
				<li class="cart-product"><h3><%= c.product.name %></h3></li>
				<li class="cart-product">por <%= c.product.user.name %></li>
				<div id="shipping-button-<%=c.id%>"> 
					<%= render('carts/shipping_button', c: c) %>
				</div>
				<!-- Fighting with this shit, from here...
				<div id="shipping-button"> 
					<% if c.shipping_boolean == true %>
						<%= render('carts/shipping_button_on', c: c) %>
					<% else %>
						<%= render('carts/shipping_button_off', c: c) %>
					<% end %>
				</div>
				<to here-->
				<p>
					<li class="cart-product">Precio: $<%= c.product.price %></li>
				</p>
				<p>
					<li class="cart-product">Envio: $<%= c.product.shipping %></li></p>

				<%= link_to '<i class="fa fa-times"></i> Borrar'.html_safe, c, class: "delete-cart-item", method: :delete %>
				<% @total = @total + c.product.price + c.product.shipping %>
			<% end %>
		</div>
		<div class="checkout-box">
			<h3 class="total-price">Cantidad de Productos:</h3>
			<h3 class="total-price"><%= current_user.carts.count %></h3>
			<h3 class="total-price">Total a Pagar:</h3>
				<h3 class="total-price">$<%= cart_total_price %></h3>
			<%# Esta linea de abajo se supone es la que tiene que mandar parámetros, más no se para qué. Esos parametros deben servirme para poder enviar datos al controlador y poder redireccionar al usuario al generar una dirección, en caso de no tenerla. #>
			<%#= link_to 'Pasar a pagar', new_orders_path.params(order_id: order.id), class: "checkout-button" %>
			<% if @carts.count > 0 %> 
				<%= link_to 'Pasar a pagar', new_order_path, class: "checkout-button" %>
			<% else %>
				<%= link_to 'No hay nada que pagar', '#', class: "checkout-button-empty" %>
			<% end %>
			<%= link_to 'Seguir comprando', root_path, class: "keep-buying-button" %>
		</div>
	</div>
</div>